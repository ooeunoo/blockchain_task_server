export enum METHOD {
  POST = 'POST',
  GET = 'GET',
  DELETE = 'DELETE',
  PUT = 'PUT',
}

export enum API_PATH_APP {
  HEARTBEAT = 'heartbeat',
  MY_IP = 'myip',
}

export enum ORDER {
  ASC = 'ASC',
  DESC = 'DESC',
}
